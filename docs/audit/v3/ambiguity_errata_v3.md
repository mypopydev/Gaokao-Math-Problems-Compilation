# 上海卷 v3 Ambiguity 勘误说明

> 本文件仅补充审计勘误说明，**不修改任何 `content/` 文件**。题面、答案和解析仍以当前仓库内容为准；下列“意图性解释”不是已确认的原题修订。

## 统一说明

- `ambiguous_problem` 表示：按当前题面无法唯一确定答案、条件不足、量词不足、图表/选项与解析冲突，或原始试卷本身存在印刷矛盾。
- 对原始扫描件已经确认确实如此的项目，仓库优先保持忠实转录，不自行替换数据或补写条件。
- 只有取得权威勘误或用户明确授权后，才可修改题面、答案或解析。

## 逐项勘误

| ID | 当前题面问题 | 意图性解释/参考 | 当前处置 |
|---|---|---|---|
| `2021/shanghai_spring#Q12` | 题面量词为 `∀n∃φ`，允许每个 n 单独选择 φ；解析却按一个固定 φ 的覆盖问题求最小值 | 若原意为 `∃φ∀n`，官方 `2π/5` 才有讨论基础 | 不修改；等待量词勘误 |
| `2022/shanghai#Q12` | 函数方程题缺少“对所有函数”或“对固定函数”的量词，`[α,+∞)` 对统一保证充分但对固定函数未必必要 | 若求所有允许函数的统一最小保证区间，可保留官方范围 | 不修改；在解析中不擅自补量词 |
| `2026/shanghai_spring#Q11` | 四焦点共圆与两椭圆四个不同实交点条件不相容；原解析的 `b²=3` 无法从字面推出 | 需官方勘误确认是焦点条件、交点条件还是参数抄录错误 | 不修改；标记源题异常 |
| `1992/shanghai#Q12` | 参数 a 没有限制，`log_a x`、`x^a` 的单调性随参数改变，选项 C 不是字面唯一结论 | 可能缺少 a 的范围或题目要求的统一参数判断 | 不修改；等待参数勘误 |
| `1996/shanghai_liberal#Q15` | 原题写“直线 OA”，允许 P 为内分点或外分点；标准答案只采用内分解 | 若原意为“线段 OA”或“P 在线段 OA 上”，则标准答案成立 | 不修改；保留两种字面解 |
| `1996/shanghai_science#Q15` | 与文科对应题相同，“直线 OA”不能排除外分点 | 同上，标准答案依赖内分解释 | 不修改；保留原题 |
| `2002/shanghai_liberal#Q16` | 图表显示温度最高月与用电量最高月存在并列，A/C 可能同时满足；解析还出现最高温月份不一致 | 需确认原图数据、选项文字或标准答案中哪一处应修正 | 不修改；保留原图与答案 |
| `2002/shanghai_science#Q16` | 与文科图表相同，存在 A/C 同时成立的字面解释 | 同上 | 不修改；等待权威裁定 |
| `2004/shanghai_liberal#Q18` | x、y 使用 m，面积印为 `8 cm²`；按字面 SI 换算和按意图理解为 `8 m²` 得不同结果 | 现代参考资料常按 `8 m²` 解读，但本地原始扫描件显示 `8 cm²` | 不修改；保留单位冲突 |
| `2004/shanghai_liberal#Q19` | 先定义 g 的定义域 D，后续小问却写 `B⊆A`，B 未定义 | 若 B 应为 D，则当前意图性区间计算可保留 | 不修改；说明 B=D 仅为推测 |
| `2004/shanghai_science#Q18` | 与文科对应题存在相同 m/cm² 单位冲突 | 等待权威确认面积单位 | 不修改；保留两种解释 |
| `2004/shanghai_science#Q19` | D/B 标号不一致，B 未定义 | 若 B 应为 D，则可沿用当前意图性推导 | 不修改；等待勘误 |
| `2004/shanghai_science#Q22` | 椭圆、P1=(3,0)、S3=255 等条件无法同时满足；原始扫描件也印有该组数据 | 需权威确认 P1、S3 或椭圆方程中的哪项有误 | 不修改；按字面标记无解 |
| `2024/shanghai_spring#Q11` | 只说明圆经过 E、F 和 AD 上一点，未说明通道必须在草地内或与 AD 相切，圆不唯一 | 若隐含“圆完全位于草地内”，官方切圆及周长 2.73 才可能成立 | 不修改；等待几何条件勘误 |
| `2015/shanghai_spring#Q1` | 原题保留“若集合则”残缺病句；虽然补集 `{3}` 可由已有集合推出，但完整命题不明 | 需恢复缺失连接词或命题 | 不修改；保留答案并标记题面残缺 |
| `2015/shanghai_spring#Q11` | 未给出组成三位数的数字集合，不同数字集合产生不同答案 | 需补充数字集合后重新计算 | 不修改；不保留唯一数值结论 |
| `2019/shanghai#Q11` | `n=1,2` 时双曲线点不存在实数坐标，解析实际静默使用充分大的 n | 若原意为 `n≥3` 或只讨论最终有效项，极限结论才可继续使用 | 不修改；等待定义域/起始条件勘误 |

## 处理边界

- 本次只新增本说明文件，未修改 `content/`。
- 未改变任何答案、解析、题面或图片资源。
- `ambiguous_problem` 不计入已确认答案错误。
- 后续如需处理某一项，应单独核对权威来源并形成独立 commit，不进行批量猜测式修订。
